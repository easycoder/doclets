RewriteEngine On

# Keep real files/directories working normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Clean secret endpoints
RewriteRule ^token/?$ secrets.php?name=token [QSA,L]
RewriteRule ^key/?$ secrets.php?name=key [QSA,L]
